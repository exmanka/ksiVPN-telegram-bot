[pytest]
# Tests scope is limited to the payments module — the only critical-path code
# in the project that handles real money. Other modules rely on manual QA.
testpaths = tests
asyncio_mode = auto
filterwarnings =
    error
    ignore::DeprecationWarning:aiohttp
    ignore::ResourceWarning
